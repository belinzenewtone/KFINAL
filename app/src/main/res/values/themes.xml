<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal XML theme. Compose drives all colours; this just sets the window
  background and provides the splash-screen parent theme.
-->
<resources>

    <style name="Theme.LifeOS" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>

    <!-- Splash screen theme — shows before Compose first frame -->
    <style name="Theme.LifeOS.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.LifeOS</item>
    </style>

</resources>
