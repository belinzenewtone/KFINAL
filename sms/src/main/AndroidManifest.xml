<?xml version="1.0" encoding="utf-8"?>
<!--
  SMS module manifest.
  Declares the BroadcastReceivers and WorkManager workers that the parser needs.
  These are merged into the app manifest automatically by the Android build system.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>

        <!-- Real-time SMS receiver -->
        <receiver
            android:name="com.lifeos.sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver — re-registers background workers after reboot -->
        <receiver
            android:name="com.lifeos.sms.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
